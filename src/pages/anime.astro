---
import { Icon } from "astro-icon/components";
import { siteConfig } from "@/config";
import MainGridLayout from "@/layouts/MainGridLayout.astro";

if (!siteConfig.pages.anime) {
	return Astro.redirect("/404/");
}

const username = siteConfig.anime?.username ?? "";
const statuses = siteConfig.anime?.statuses ?? [
	"watching",
	"completed",
	"plan_to_watch",
];
const baseUrl = import.meta.env.BASE_URL || "/";
const normalizedBaseUrl = baseUrl.endsWith("/") ? baseUrl : `${baseUrl}/`;
const animeApiUrl = `${normalizedBaseUrl}api/anime.json`;
---

<MainGridLayout title="MyAnimeList" description="Anime I am watching and completed">
	<div class="flex w-full rounded-(--radius-large) overflow-hidden relative min-h-32">
		<div class="card-base z-10 px-9 py-6 relative w-full">
			<div class="relative w-full mb-8">
				<div class="mb-6">
					<div class="flex items-center gap-3 mb-3">
						<div class="h-8 w-8 rounded-lg bg-(--primary) flex items-center justify-center text-white dark:text-black/70">
							<Icon name="material-symbols:animation" class="text-[1.5rem]" />
						</div>
						<div class="text-3xl font-bold text-neutral-900 dark:text-neutral-100">
							MyAnimeList
						</div>
					</div>
					<p class="text-base text-neutral-600 dark:text-neutral-400 leading-relaxed">
						Anime from my MAL watching, completed, and plan-to-watch lists.
					</p>
					<p id="anime-updated-at" class="text-xs text-neutral-500 dark:text-neutral-500 mt-2">
						Loading latest list from MyAnimeList...
					</p>
				</div>
			</div>

			{!username ? (
				<div class="text-center py-16">
					<div class="inline-flex items-center justify-center w-16 h-16 bg-(--btn-regular-bg) rounded-full mb-6 border border-(--line-divider)">
						<Icon name="material-symbols:key" class="text-[2rem] text-(--btn-content)" />
					</div>
					<h2 class="text-xl font-semibold text-black/80 dark:text-white/80 mb-3">
						MyAnimeList is not configured
					</h2>
					<p class="text-black/60 dark:text-white/60 mb-4 max-w-xl mx-auto">
						Set <code>siteConfig.anime.username</code>.
					</p>
				</div>
			) : (
				<div
					id="anime-app"
					data-loading="true"
					data-api-url={animeApiUrl}
					data-statuses={JSON.stringify(statuses)}
				>
					<div id="anime-filters" class="flex flex-wrap gap-2 mb-6"></div>
					<div id="anime-grid" class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-5"></div>
					<div id="anime-pagination" class="flex items-center justify-center gap-2 mt-8"></div>
					<div id="anime-empty" class="hidden text-center py-16">
						<div class="inline-flex items-center justify-center w-16 h-16 bg-(--btn-regular-bg) rounded-full mb-6 border border-(--line-divider)">
							<Icon name="material-symbols:error-outline" class="text-[2rem] text-(--btn-content)" />
						</div>
						<h2 class="text-xl font-semibold text-black/80 dark:text-white/80 mb-3">
							No anime loaded
						</h2>
						<p id="anime-empty-message" class="text-black/60 dark:text-white/60 mb-4 max-w-xl mx-auto">
							Check your MAL list visibility and client ID.
						</p>
					</div>
				</div>
			)}

			<p class="text-xs text-black/45 dark:text-white/45 mt-8">
				Anime list data is provided by MyAnimeList.
			</p>
		</div>
	</div>
</MainGridLayout>

<style>
	.line-clamp-2 {
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}
</style>
